# Public origin of the live site (https, no trailing slash). Required for production builds:
# it is used for canonical tags, sitemap.xml, robots.txt, llms.txt, structured data and social images.
VITE_SITE_URL=
